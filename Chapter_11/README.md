## Chapter 11 - Kernel Firmware

+ `git` to be able to install firmare
+ `linux-firmware` The kernel or kernel drivers look for firmware images in `/lib/firmware`.
+ `wireless-regdb` Firmware for Regulatory Database of Wireless Devices
+ `microcode` Early loading of intel microcode
+ `sof-bin` Sound Open Firmware for laptops and mobile intel chips

## Final Release Files

`11-z-the-end.sh`
