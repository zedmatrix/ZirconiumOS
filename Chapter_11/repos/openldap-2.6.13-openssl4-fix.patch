--- a/libraries/libldap/tls_o.c	2026-05-22 08:52:52.549596407 -0700
+++ b/libraries/libldap/tls_o.c	2026-05-22 09:18:50.250567718 -0700
@@ -194,8 +194,8 @@
 		ldap_charray_free( dirs );
 	}
 	if ( cert ) {
-		X509_NAME *xn = X509_get_subject_name( cert );
-		xn = X509_NAME_dup( xn );
+		const X509_NAME *cxn = X509_get_subject_name( cert );
+		X509_NAME *xn = X509_NAME_dup( cxn );
 		if ( !ca_list )
 			ca_list = sk_X509_NAME_new_null();
 		if ( xn && ca_list )
@@ -751,7 +751,7 @@
 {
 	tlso_session *s = (tlso_session *)sess;
 	X509 *x;
-	X509_NAME *xn;
+	const X509_NAME *xn;
 
 	x = SSL_get_certificate( s );
 
@@ -861,7 +861,7 @@
 	if (chkSAN) {
 	i = X509_get_ext_by_NID(x, NID_subject_alt_name, -1);
 	if (i >= 0) {
-		X509_EXTENSION *ex;
+		const X509_EXTENSION *ex;
 		STACK_OF(GENERAL_NAME) *alt;
 
 		ex = X509_get_ext(x, i);
@@ -967,10 +967,10 @@
 	}
 
 	if (ret != LDAP_SUCCESS) {
-		X509_NAME *xn;
-		X509_NAME_ENTRY *ne;
+		const X509_NAME *xn;
+		const X509_NAME_ENTRY *ne;
 		ASN1_OBJECT *obj;
-		ASN1_STRING *cn = NULL;
+		const ASN1_STRING *cn = NULL;
 		int navas;
 
 		/* find the last CN */
@@ -999,11 +999,11 @@
 			ld->ld_error = LDAP_STRDUP(
 				_("TLS: unable to get CN from peer certificate"));
 
-		} else if ( cn->length == nlen &&
-			strncasecmp( name, (char *) cn->data, nlen ) == 0 ) {
+		} else if ( ASN1_STRING_length(cn) == nlen &&
+			strncasecmp( name, (char *) ASN1_STRING_get0_data(cn), nlen ) == 0 ) {
 			ret = LDAP_SUCCESS;
 
-		} else if (( cn->data[0] == '*' ) && ( cn->data[1] == '.' )) {
+		} else if (( ASN1_STRING_get0_data(cn)[0] == '*' ) && ( ASN1_STRING_get0_data(cn)[1] == '.' )) {
 			char *domain = strchr(name, '.');
 			if( domain ) {
 				int dlen;
@@ -1011,8 +1011,8 @@
 				dlen = nlen - (domain-name);
 
 				/* Is this a wildcard match? */
-				if ((dlen == cn->length-1) &&
-					!strncasecmp(domain, (char *) &cn->data[1], dlen)) {
+				if ((dlen == ASN1_STRING_length(cn)-1) &&
+					!strncasecmp(domain, (char *) &ASN1_STRING_get0_data(cn)[1], dlen)) {
 					ret = LDAP_SUCCESS;
 				}
 			}
@@ -1021,7 +1021,7 @@
 		if( ret == LDAP_LOCAL_ERROR ) {
 			Debug3( LDAP_DEBUG_ANY, "TLS: hostname (%s) does not match "
 				"common name in certificate (%.*s).\n", 
-				name, cn->length, cn->data );
+				name, ASN1_STRING_length(cn), ASN1_STRING_get0_data(cn) );
 			ret = LDAP_CONNECT_ERROR;
 			if ( ld->ld_error ) {
 				LDAP_FREE( ld->ld_error );
@@ -1561,8 +1561,8 @@
 	X509 *cert;
 	int errnum;
 	int errdepth;
-	X509_NAME *subject;
-	X509_NAME *issuer;
+	const X509_NAME *subject;
+	const X509_NAME *issuer;
 	char *sname;
 	char *iname;
 	char *certerr = NULL;
--- a/servers/slapd/overlays/autoca.c
+++ b/servers/slapd/overlays/autoca.c
@@ -44,9 +44,13 @@
 
 #if OPENSSL_VERSION_NUMBER >= 0x10100000
 #include <openssl/rsa.h>
+#ifndef X509_get_notBefore
 #define X509_get_notBefore(x)	X509_getm_notBefore(x)
+#endif
+#ifndef X509_get_notAfter
 #define X509_get_notAfter(x)	X509_getm_notAfter(x)
 #endif
+#endif
 
 #if OPENSSL_VERSION_MAJOR >= 3
 #define BN_pseudo_rand(bn, bits, top, bottom)	BN_rand(bn, bits, top, bottom)
@@ -272,7 +276,8 @@ typedef struct genargs {
 
 static int autoca_gencert( Operation *op, genargs *args )
 {
-	X509_NAME *subj_name, *issuer_name;
+	X509_NAME *subj_name;
+	const X509_NAME *issuer_name;
 	X509 *subj_cert;
 	struct berval derdn;
 	unsigned char *pp;
-- 
