pkgdir=[flex-2.6.4]
pkgname=[flex]
pkgver=[2.6.4]
pkgrel=[1]
zarchive=[true]
pkgurl=[https://github.com/westes/flex/releases/download/v2.6.4/flex-2.6.4.tar.gz]
md5sum=[2882e3179748cc9f9c23ec593d6adc8d]

build=[./configure --prefix=/usr --docdir=/usr/share/doc/flex-2.6.4 --disable-static && make]

check=[make check]

install=[make install]

post=[ln -sv flex /usr/bin/lex && ln -sv flex.1 /usr/share/man/man1/lex.1 ]
