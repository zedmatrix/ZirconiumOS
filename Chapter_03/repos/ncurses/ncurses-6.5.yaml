package:
    name: ncurses
    version: 6.5
    rel: 1
    archive: false
    delete: true

sources:
    - url: https://invisible-island.net/archives/ncurses/current/ncurses-6.5-20251004.tgz
      md5: ec5bb613507c60a470105e269a91651f
      sha256: da13d75fbfa75b9a52e28900b6369d5a73f8b6946e0a777e8cc1cd2923bb7e9c

prepare: |
    ./configure --prefix=/usr --mandir=/usr/share/man --with-shared --without-debug \
    --without-normal --with-cxx-shared --enable-pc-files --with-pkg-config-libdir=/usr/lib/pkgconfig

build: |
    make

install: |
    make DESTDIR=$PWD/dest install &&
    install -vm755 dest/usr/lib/libncursesw.so.6.5 /usr/lib &&
    rm -v dest/usr/lib/libncursesw.so.6.5 &&
    sed -e 's/^#if.*XOPEN.*$/#if 1/' -i dest/usr/include/curses.h &&
    cp -av dest/* / &&
    ln -sfv libncursesw.so /usr/lib/libcurses.so && cp -v -R doc -T /usr/share/doc/ncurses-6.5-20250809 &&
    for lib in ncurses form panel menu ; do
    ln -sfv lib${lib}w.so /usr/lib/lib${lib}.so
    ln -sfv ${lib}w.pc /usr/lib/pkgconfig/${lib}.pc
    done

required:
    - gcc
